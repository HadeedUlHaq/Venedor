{
  "name": "bootstrap_install",
  "version": "1.0.0",
  "description": "bootstrap with npm",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass --watch --update --style=expanded sass:css",
    "sass-prod": "sass --no-source-map --style=compressed sass:css"
  },
  "author": "Hadeed Ul Haq",
  "license": "ISC",
  "dependencies": {
    "@fontsource/roboto": "^4.5.3",
    "@fortawesome/fontawesome-free": "^5.15.4",
    "bootstrap": "^5.1.3",
    "bootstrap-icons": "^1.8.0",
    "jquery": "^3.6.0",
    "slick-carousel": "^1.8.1"
  }
}
